---
wg_server_private_key: "{{ vault_wg_server_private_key }}"
wg_server_public_key: "{{ vault_wg_server_public_key }}"
wg_server_port: "{{ vault_wg_server_port }}"
wg_subnet: "{{ vault_wg_subnet }}"
wg_interface_address: "{{ vault_wg_interface_address }}"
wg_list_peers:
  - public_key: "{{ vault_wg_client_public_key_1 }}"
    networks: "{{ vault_wg_peer_networks_1 }}"
wg_interface_name: "wg0"
external_port_name: "ens3"

enable_nat: false
